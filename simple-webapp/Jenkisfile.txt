pipeline {
    agent any
    tools {
        maven 'M3' // Ensure Maven is configured in Jenkins
    }
    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/your-username/simple-webapp.git'
            }
        }
        stage('Build & Test') {
            steps {
                sh 'mvn clean package' // This will fail due to the error in JSP
            }
        }
        stage('Deploy (Optional)') {
            steps {
                echo 'Deployment would happen here if build succeeded.'
            }
        }
    }
    post {
        failure {
            slackSend channel: '#alerts', message: '❌ Build Failed! Check Jenkins.'
            // Or use `mail` for email notifications
        }
    }
}